import React, {useState} from "react";
import {
    Box,
    Container
} from "@mui/material";

function LoginPage() {
    return (
        <Box
            sx={{
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'center',
            }}
        >
            <Container
            >

            </Container>
        </Box>
    );
}

export default LoginPage;